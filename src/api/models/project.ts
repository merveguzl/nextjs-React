export type ProjectItem = {
  projectImage: string;
  projectName: string;
  subTitle: string;
  title: string;
  time: string;
  url: string;
  desc: string;
};

export type ProjectResponse = ProjectItem[];
